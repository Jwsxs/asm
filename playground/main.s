	.DATA
DOUBLE:	.DOUBLE 2.0
SINGLE:	.FLOAT 1.0
	
	.TEXT
	.GLOBL _start

_start:
	PUSH %RBP
	MOVQ %RSP, %RBP

#	MOVL $, %xmm0
	MOVSD DOUBLE(%RIP), %XMM0 # DOUBLE
	MOVSS SINGLE(%RIP), %XMM1 # SINGLE
	
_finish:
	MOVQ $0X3C, %RAX
	XOR %RDI, %RDI
	SYSCALL
